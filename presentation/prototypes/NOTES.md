# Aesthetic bake-off — PROTOTYPE (throwaway)

**Question:** Which visual/animation direction for the "Git Worktrees: One Repo, N Coding Agents" lightning talk?

**How to view:** open `prototypes/index.html` in a browser. Bottom bar (or keys `1`/`2`/`3`) switches variant; `←`/`→` (or click) moves through slides.

Three variants, same 5-beat storyline (title → collision → worktree split → wiring agents → merge back), so it's apples-to-apples:

| Key | Variant | Direction |
| --- | -------------------- | --------------------------------------------------------------- |
| A   | Multiplayer IDE      | Collaborative-editor chrome, Figma-style colored presence cursors |
| B   | Living Git Graph     | Animated `git log --graph` SVG commit graph as the through-line   |
| C   | tmux Command Center  | Retro CRT terminal, tmux pane grid, blinking block cursors        |

Agent color identity (shared): Claude `#D97757` · Codex `#2DB88E` · Gemini `#8E7CF0`.

## Verdict

_TBD — pick a winner (or "header from B, cursors from A" style mix), then fold into the real Slidev deck and delete this dir._
